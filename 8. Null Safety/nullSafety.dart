void main(){

// int a; //before use we have to define a with value             //Normal null
int? a; //before use we have to define a with value           //Null safety with ?
int b=1;
// print(a+b);  //null variable can't perform operation

print(a);
}